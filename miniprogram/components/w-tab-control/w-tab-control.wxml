<view class="tab-control">
  <block wx:for="{{titles}}" wx:key="">
    <view class="tab-item {{currentIndex == index ?'active':''}}" bind:tap="handleCurrentTarget" data-id="{{index}}">
    <text>{{item}}</text></view>
  </block>
</view>